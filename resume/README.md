# Resume

This directory contains the LaTeX source and local font files used to generate the public resume PDF.

Build from the repository root:

```sh
tectonic --outdir static resume/resume.tex
```

The generated file is written to `static/resume.pdf`, where it is served by the portfolio.
